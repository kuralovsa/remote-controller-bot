# 🔧 Telegram Remote File Manager Bot
Мощный Telegram-бот для полного удалённого управления компьютером (Windows).  
Позволяет работать с файлами, папками, запускать команды, просматривать содержимое, загружать файлы и выполнять Python-скрипты — всё прямо из Telegram.

## ✨ Возможности
### 📁 Файловый менеджер (GUI в одном сообщении)
- Просмотр структуры дисков
- Открытие папок
- Просмотр файлов
- Скачивание файлов
- Просмотр текста напрямую в Telegram
- Удаление файлов/папок
- Навигация ↑ Назад / Вперёд / Обновить
- Полностью интерактивное меню без спама сообщений  
- Callback-данные короткие (`fm_123`), безопасные и быстрые

### 🧭 Навигация по системе
- `/pwd` — показать текущую директорию
- `/cd <path>` — перейти
- `/ls` — открыть файловый менеджер
- `/back` / `/forward` — история переходов  
(работает как в браузере)

### 📤 Загрузка файлов в компьютер
- `/upload_to <folder>` — выбрать директорию
- отправляете файл в чат → бот сохраняет на диск

### 🧰 Работа с файлами
- `/mkdir <folder>` — создать папку
- `/touch <file>` — создать файл
- `/rm <path>` — удалить
- `/mv <src> <dst>` — переместить
- `/rename <old> <new>` — переименовать
- `/zip <path>` — упаковать в ZIP
- `/open <file>` — посмотреть текст файла
- `/find <mask>` — поиск (`*.txt`, `*.jpg`)

### 🖥 Выполнение команд Windows
